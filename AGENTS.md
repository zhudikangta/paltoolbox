# 游戏工具箱项目规则

这是一个纯前端网站，使用 `HTML`（页面结构）、`CSS`（页面样式）和 `JavaScript`（网页程序），不使用前端框架。项目自有的全局名称统一使用 `PT_`（幻兽帕鲁工具箱的公共前缀）。

## 一、规则适用范围

- 本文件约束“游戏工具箱”目录及其全部子目录。
- 旧项目的规则不自动带入本项目。
- 查看、搜索、分析和解释属于只读操作，可以直接进行。
- 第一次修改文件前，必须用大白话复述需求并得到用户明确回复“做”。
- 用户批准后，在已说明的范围内可以连续完成，不需要每一步重新询问。
- 如果需要扩大范围、删除重要资料、覆盖用户改动、发布网站或者进行其他不可轻易撤销的操作，必须重新说明并等待确认。

## 二、动手前必须说明

修改前必须回答：

1. 本次属于新工具、修改现有效果、新功能、错误修复、数据更新还是架构整理。
2. 项目中是否已有能够复用的通用模块。
3. 会修改哪些范围，不会修改哪些范围。

已有通用能力必须复用。禁止为了省事，在不同工具中复制同一套读取、筛选、窗口或者状态逻辑。

## 三、代码修改原则

- 修改现有效果时，先找到产生效果的原始代码，再替换原始实现。
- 禁止在旧样式后面不断追加覆盖规则来掩盖问题。
- 原则上不使用 `!important`（强制提高样式优先级）。只有修正无法控制的外部库样式时可以使用，并必须说明原因。
- 条件判断可以用于真实业务分支，但不能用来把废弃逻辑长期包在里面。
- 页面容器只有在承担明确布局或语义职责时才能新增，不能单纯为了绕过旧结构而套壳。
- 搜索确认无人引用的旧代码应删除；仍在迁移中的旧入口必须标明迁移范围，完成后删除。
- 不改与当前任务无关的用户内容，不使用破坏性的版本回退命令。

## 四、工具结构与复用

复杂工具优先按以下职责拆分：

```text
工具名称
├── 核心：纯计算和数据转换，不操作页面
├── 通用逻辑：状态、调用核心、组织界面数据
├── 桌面模式适配：桌面卡片的显示和绑定
├── 网页模式适配：完整网页的显示和绑定
└── 样式：工具内部专用外观
```

- 简单工具可以省略确实没有职责的层，不得为了满足目录形式建立空文件。
- 两种运行模式共用的逻辑必须放在通用逻辑中。
- 能独立计算或需要自动检查的逻辑必须放在核心中。
- 多个工具都会使用的能力放入共享目录。

## 五、数据治理

数据按职责分为四类：

1. 原始来源：保存获取时的原貌，只作为证据，不给网站直接读取。
2. 标准事实：清洗、统一编号并解决冲突后的正式资料，是网站唯一可信入口。
3. 生成索引：从标准事实自动生成的搜索、筛选和反向关系，不允许人工直接修改。
4. 研究数据：本站实测、公式推导和验证记录，必须区分已证实、推测和待验证。

必须遵守：

- 正式事实优先使用 `JSON`（结构化数据文件）保存，程序逻辑使用 `JavaScript`（网页程序）保存。
- 工具不得在运行时直接依赖外部资料站，外部资料必须先进入原始来源，再转换为本站标准事实。
- 每批数据必须记录来源、获取日期、对应游戏版本和转换版本。
- 解包原始值、游戏显示值和实测真实值不得混成一个字段。
- 不同来源发生冲突时必须生成冲突记录，禁止静默覆盖。
- 外部资料中的客观事实可以按来源和授权情况整理；对方的原创文字、翻译表达、页面编排和独创分类不得直接冒充本站原创。
- 自动生成文件必须在文件头或配套说明中标明生成来源，修改生成规则后重新生成。

## 六、术语和汇报

- 面向用户的说明使用大白话。
- 英文、缩写和不常见术语在同一轮第一次出现时给出中文解释，后续不重复解释。
- 代码、命令、文件路径和原始错误信息不要求逐项插入解释，但必须在其后说明整体含义。
- 完成后必须说明这是全局改动还是某个工具内部的改动，并说明改了什么、删了什么、为什么这样改。

## 七、验证要求

- 修改行为前先建立能够失败的自动检查，再写最小实现使检查通过。
- 配置文件、纯说明文件和自动生成文件可以不做测试先行，但仍需检查格式和引用。
- 完成后同时提供自动检查结果和肉眼验证方法。
- 肉眼验证必须写明打开哪个页面、点击什么、输入什么以及应该看到什么。
- 不能只说“测试通过”，也不能只凭肉眼判断计算和数据关系正确。

## 八、结构地图同步

只有以下变化需要更新本文件中的结构地图：

- 新建或删除正式目录。
- 新建或删除工具。
- 新建或删除全局通用模块。
- 正式数据入口发生变化。

普通测试文件、同目录内的小型辅助文件、临时报告和自动生成结果不需要逐项列入结构地图。

```text
游戏工具箱
├── AGENTS.md：本项目工作规则和结构地图
├── docs：设计与实施计划
├── 迁移验证：迁移程序和完整性检查
└── PalToolbox：网站主体
    ├── 入口页面：浏览器打开的主入口
    ├── 运行模式：桌面、网页、沉浸和空间等展示方式
    ├── 共享：跨工具复用的框架、设置和视觉能力
    └── 游戏内容
        ├── 幻兽帕鲁：程序、界面适配、外部库和不随版本变化的界面资源
        └── 幻兽帕鲁1.0：1.0 版本正式数据、原始来源、地图瓦片和游戏图标资源
```
